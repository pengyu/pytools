source('../intersect2.R')

intersect2(
  list(
    c('a', 'b', 'c')
    , c('a', 'b', 'd')
    , c('a', 'f', 'g')
    )
  )

intersect2(
  c('a', 'b', 'c')
  , c('a', 'b', 'd')
  , c('a', 'f', 'g')
  )

